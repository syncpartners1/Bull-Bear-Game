# Copy this to .env and fill in values before running locally
# Railway sets these automatically in production

NODE_ENV=production
PORT=3001

# Set this in Railway dashboard → Variables
# Used by the client to connect to the server via Socket.io
# In production (single service) leave blank — client connects to same origin
VITE_SERVER_URL=
